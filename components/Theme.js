import { createTheme } from '@mui/material'

export default createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#009688',
    },
    secondary: {
      main: '#546e7a',
    },
    background: {
      default: '#e0f2f1',
    },
  },
})
