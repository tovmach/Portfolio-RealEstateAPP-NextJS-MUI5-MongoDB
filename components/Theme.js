import { createTheme } from '@mui/material'

export default createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#1A626D',
    },
    secondary: {
      main: '#2DA7BA',
    },
    background: {
      default: '#EBF1F5',
    },
  },
})
